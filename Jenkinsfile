pipeline {
	agent any
	stages {
		stage("Cleaning Stage") {
			
			steps {
				cat "mvn clean"
			}
		}
		stage("Building Stage") {
			
			steps {
				cat "mvn clean"
			}
		}
		stage("Packaging Stage") {
			
			steps {
				cat "mvn clean"
			}
		}
	}
}
